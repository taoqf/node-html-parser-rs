FROM docker.cnb.cool/mmstudio666/images/rust:latest

RUN rustup target add wasm32-unknown-unknown && cargo install wasm-pack wasm-bindgen-cli

WORKDIR /mm

ADD . .

# !!! 在Windows上，当前的docker版本会映射错本地路径
# ENTRYPOINT [ "./.devcontainer/watch.sh" ]

EXPOSE 3000
